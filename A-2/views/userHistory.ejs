<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User History</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar', { page: '/userHistory' }) %>
    <div class="container mx-auto p-4">
        <h2 class="text-2xl font-bold mb-4">User History</h2>
        <div class="bg-white p-4 rounded-md shadow-md">
            <h3 class="text-xl font-semibold mb-2">Welcome, <%= session.user.userName %>!</h3>
            <p class="text-gray-600 mb-4">Email: <%= session.user.email %></p>
            <table class="min-w-full divide-y divide-gray-200">
                <thead>
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Login Date/Time</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Client Information</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    <% session.user.loginHistory.forEach(history => { %>
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap"><%= new Date(history.date).toLocaleString() %></td>
                            <td class="px-6 py-4 whitespace-nowrap"><%= history.userAgent %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
